{
  "title": "slider: Cannot move handles when they are stacked (min === max)",
  "primary": "negative",
  "emotions": ["frustration"],
  "score": 0.8,
  "explanation": "This issue appears to be reporting a bug with a slider component, where the handles cannot be moved when they are stacked (min === max). This is likely a frustrating issue for the user, as it prevents them from using the slider functionality as expected."
}
