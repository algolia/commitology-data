{
  "title": "router UiState does not receive `configure.filters` values when set with `setIndexUiState`.",
  "primary": "negative",
  "emotions": ["frustration", "confusion"],
  "score": 0.8,
  "explanation": "The issue describes a bug where the 'filters' values are not being correctly saved to the URL when using the `setIndexUiState` method. This is causing frustration and confusion for the user, as they expect the filters to be persisted in the URL."
}
