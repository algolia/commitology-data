{
  "title": "Calling `setUiState` directly does not trigger `onStateChange` when defined.",
  "primary": "negative",
  "emotions": ["frustration"],
  "score": 0.8,
  "explanation": "This is a bug report describing a technical issue with the library's behavior, which is preventing the expected functionality from working as intended. The user is expressing frustration that the `onStateChange` handler is not being triggered when `setUiState` is called directly, even though the state is being updated."
}
