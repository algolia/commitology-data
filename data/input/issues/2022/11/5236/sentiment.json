{
  "title": "`createURL` always returns root/empty url on first render",
  "primary": "negative",
  "emotions": ["frustration"],
  "score": 0.8,
  "explanation": "The issue describes a bug where the `createURL` function is not working as expected, resulting in incorrect URLs being generated on the server. This is a blocking issue that is causing frustration for the user."
}
