{
  "hash": "854a2c2",
  "author": "Haroen Viaene",
  "subject": "chore(ts): remove undefined type declaration in JSDoc (#4266)",
  "bodyLines": [
    "Connector isn't defined here, so it caused an error when used in Angular InstantSearch.\r",
    "\r",
    "<details><summary>Before:</summary>\r",
    "\r",
    "```ts\r",
    "export default connectGeoSearch;\r",
    "export type LatLng = {\r",
    "    /**\r",
    "     * The latitude in degrees.\r",
    "     */\r",
    "    lat: number;\r",
    "    /**\r",
    "     * The longitude in degrees.\r",
    "     */\r",
    "    lng: number;\r",
    "};\r",
    "export type Bounds = {\r",
    "    /**\r",
    "     * The top right corner of the map view.\r",
    "     */\r",
    "    northEast: LatLng;\r",
    "    /**\r",
    "     * The bottom left corner of the map view.\r",
    "     */\r",
    "    southWest: LatLng;\r",
    "};\r",
    "export type CustomGeoSearchWidgetOptions = {\r",
    "    /**\r",
    "     * If true, refine will be triggered as you move the map.\r",
    "     */\r",
    "    enableRefineOnMapMove?: boolean;\r",
    "    /**\r",
    "     * Function to transform the items passed to the templates.\r",
    "     */\r",
    "    transformItems?: (arg0: any[]) => any[];\r",
    "};\r",
    "export type GeoSearchRenderingOptions = {\r",
    "    /**\r",
    "     * The matched hits from Algolia API.\r",
    "     */\r",
    "    items: any[];\r",
    "    /**\r",
    "     * The current position of the search.\r",
    "     */\r",
    "    position?: LatLng;\r",
    "    /**\r",
    "     * The current bounding box of the search.\r",
    "     */\r",
    "    currentRefinement: Bounds;\r",
    "    /**\r",
    "     * Sets a bounding box to filter the results from the given map bounds.\r",
    "     */\r",
    "    refine: (arg0: Bounds) => any;\r",
    "    /**\r",
    "     * Reset the current bounding box refinement.\r",
    "     */\r",
    "    clearMapRefinement: () => any;\r",
    "    /**\r",
    "     * Return true if the current refinement is set with the map bounds.\r",
    "     */\r",
    "    isRefinedWithMap: () => boolean;\r",
    "    /**\r",
    "     * Toggle the fact that the user is able to refine on map move.\r",
    "     */\r",
    "    toggleRefineOnMapMove: () => any;\r",
    "    /**\r",
    "     * Return true if the user is able to refine on map move.\r",
    "     */\r",
    "    isRefineOnMapMove: () => boolean;\r",
    "    /**\r",
    "     * Set the fact that the map has moved since the last refinement, should be call on each map move. The call to the function triggers a new rendering only when the value change.\r",
    "     */\r",
    "    setMapMoveSinceLastRefine: () => any;\r",
    "    /**\r",
    "     * Return true if the map has move since the last refinement.\r",
    "     */\r",
    "    hasMapMoveSinceLastRefine: () => boolean;\r",
    "    /**\r",
    "     * All original `CustomGeoSearchWidgetOptions` forwarded to the `renderFn`.\r",
    "     */\r",
    "    widgetParams: any;\r",
    "};\r",
    "/**\r",
    " * @typedef {Object} LatLng\r",
    " * @property {number} lat The latitude in degrees.\r",
    " * @property {number} lng The longitude in degrees.\r",
    " */\r",
    "/**\r",
    " * @typedef {Object} Bounds\r",
    " * @property {LatLng} northEast The top right corner of the map view.\r",
    " * @property {LatLng} southWest The bottom left corner of the map view.\r",
    " */\r",
    "/**\r",
    " * @typedef {Object} CustomGeoSearchWidgetOptions\r",
    " * @property {boolean} [enableRefineOnMapMove=true] If true, refine will be triggered as you move the map.\r",
    " * @property {function(object[]):object[]} [transformItems] Function to transform the items passed to the templates.\r",
    " */\r",
    "/**\r",
    " * @typedef {Object} GeoSearchRenderingOptions\r",
    " * @property {Object[]} items The matched hits from Algolia API.\r",
    " * @property {LatLng} position The current position of the search.\r",
    " * @property {Bounds} currentRefinement The current bounding box of the search.\r",
    " * @property {function(Bounds)} refine Sets a bounding box to filter the results from the given map bounds.\r",
    " * @property {function()} clearMapRefinement Reset the current bounding box refinement.\r",
    " * @property {function(): boolean} isRefinedWithMap Return true if the current refinement is set with the map bounds.\r",
    " * @property {function()} toggleRefineOnMapMove Toggle the fact that the user is able to refine on map move.\r",
    " * @property {function(): boolean} isRefineOnMapMove Return true if the user is able to refine on map move.\r",
    " * @property {function()} setMapMoveSinceLastRefine Set the fact that the map has moved since the last refinement, should be call on each map move. The call to the function triggers a new rendering only when the value change.\r",
    " * @property {function(): boolean} hasMapMoveSinceLastRefine Return true if the map has move since the last refinement.\r",
    " * @property {Object} widgetParams All original `CustomGeoSearchWidgetOptions` forwarded to the `renderFn`.\r",
    " * @property {LatLng} [position] The current position of the search.\r",
    " */\r",
    "/**\r",
    " * The **GeoSearch** connector provides the logic to build a widget that will display the results on a map. It also provides a way to search for results based on their position. The connector provides functions to manage the search experience (search on map interaction or control the interaction for example).\r",
    " *\r",
    " * @requirements\r",
    " *\r",
    " * Note that the GeoSearch connector uses the [geosearch](https://www.algolia.com/doc/guides/searching/geo-search) capabilities of Algolia. Your hits **must** have a `_geoloc` attribute in order to be passed to the rendering function.\r",
    " *\r",
    " * Currently, the feature is not compatible with multiple values in the _geoloc attribute.\r",
    " *\r",
    " * @type {Connector}\r",
    " * @param {function(GeoSearchRenderingOptions, boolean)} renderFn Rendering function for the custom **GeoSearch** widget.\r",
    " * @param {function} unmountFn Unmount function called when the widget is disposed.\r",
    " * @return {function(CustomGeoSearchWidgetOptions)} Re-usable widget factory for a custom **GeoSearch** widget.\r",
    " * @staticExample\r",
    " * // This example use Leaflet for the rendering, be sure to have the library correctly setup\r",
    " * // before trying the demo. You can find more details in their documentation (link below).\r",
    " * // We choose Leaflet for the example but you can use any libraries that you want.\r",
    " * // See: http://leafletjs.com/examples/quick-start\r",
    " *\r",
    " * let map = null;\r",
    " * let markers = [];\r",
    " *\r",
    " * // custom `renderFn` to render the custom GeoSearch widget\r",
    " * function renderFn(GeoSearchRenderingOptions, isFirstRendering) {\r",
    " *   const { items, widgetParams } = GeoSearchRenderingOptions;\r",
    " *\r",
    " *   if (isFirstRendering) {\r",
    " *     map = L.map(widgetParams.container);\r",
    " *\r",
    " *     L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {\r",
    " *       attribution:\r",
    " *         '&copy; <a href=\"http://osm.org/copyright\">OpenStreetMap</a> contributors',\r",
    " *     }).addTo(map);\r",
    " *   }\r",
    " *\r",
    " *   markers.forEach(marker => marker.remove());\r",
    " *\r",
    " *   markers = items.map(({ _geoloc }) =>\r",
    " *     L.marker([_geoloc.lat, _geoloc.lng]).addTo(map)\r",
    " *   );\r",
    " *\r",
    " *   if (markers.length) {\r",
    " *     map.fitBounds(L.featureGroup(markers).getBounds());\r",
    " *   }\r",
    " * }\r",
    " *\r",
    " * // connect `renderFn` to GeoSearch logic\r",
    " * const customGeoSearch = instantsearch.connectors.connectGeoSearch(renderFn);\r",
    " *\r",
    " * // mount widget on the page\r",
    " * search.addWidgets([\r",
    " *   customGeoSearch({\r",
    " *     container: document.getElementById('custom-geo-search'),\r",
    " *   })\r",
    " * ]);\r",
    " */\r",
    "declare const connectGeoSearch: Connector;\r",
    "```\r",
    "\r",
    "</details>\r",
    "\r",
    "<details><summary>After:</summary>\r",
    "\r",
    "```ts\r",
    "export default connectGeoSearch;\r",
    "export type LatLng = {\r",
    "    /**\r",
    "     * The latitude in degrees.\r",
    "     */\r",
    "    lat: number;\r",
    "    /**\r",
    "     * The longitude in degrees.\r",
    "     */\r",
    "    lng: number;\r",
    "};\r",
    "export type Bounds = {\r",
    "    /**\r",
    "     * The top right corner of the map view.\r",
    "     */\r",
    "    northEast: LatLng;\r",
    "    /**\r",
    "     * The bottom left corner of the map view.\r",
    "     */\r",
    "    southWest: LatLng;\r",
    "};\r",
    "export type CustomGeoSearchWidgetOptions = {\r",
    "    /**\r",
    "     * If true, refine will be triggered as you move the map.\r",
    "     */\r",
    "    enableRefineOnMapMove?: boolean;\r",
    "    /**\r",
    "     * Function to transform the items passed to the templates.\r",
    "     */\r",
    "    transformItems?: (arg0: any[]) => any[];\r",
    "};\r",
    "export type GeoSearchRenderingOptions = {\r",
    "    /**\r",
    "     * The matched hits from Algolia API.\r",
    "     */\r",
    "    items: any[];\r",
    "    /**\r",
    "     * The current position of the search.\r",
    "     */\r",
    "    position?: LatLng;\r",
    "    /**\r",
    "     * The current bounding box of the search.\r",
    "     */\r",
    "    currentRefinement: Bounds;\r",
    "    /**\r",
    "     * Sets a bounding box to filter the results from the given map bounds.\r",
    "     */\r",
    "    refine: (arg0: Bounds) => any;\r",
    "    /**\r",
    "     * Reset the current bounding box refinement.\r",
    "     */\r",
    "    clearMapRefinement: () => any;\r",
    "    /**\r",
    "     * Return true if the current refinement is set with the map bounds.\r",
    "     */\r",
    "    isRefinedWithMap: () => boolean;\r",
    "    /**\r",
    "     * Toggle the fact that the user is able to refine on map move.\r",
    "     */\r",
    "    toggleRefineOnMapMove: () => any;\r",
    "    /**\r",
    "     * Return true if the user is able to refine on map move.\r",
    "     */\r",
    "    isRefineOnMapMove: () => boolean;\r",
    "    /**\r",
    "     * Set the fact that the map has moved since the last refinement, should be call on each map move. The call to the function triggers a new rendering only when the value change.\r",
    "     */\r",
    "    setMapMoveSinceLastRefine: () => any;\r",
    "    /**\r",
    "     * Return true if the map has move since the last refinement.\r",
    "     */\r",
    "    hasMapMoveSinceLastRefine: () => boolean;\r",
    "    /**\r",
    "     * All original `CustomGeoSearchWidgetOptions` forwarded to the `renderFn`.\r",
    "     */\r",
    "    widgetParams: any;\r",
    "};\r",
    "declare function connectGeoSearch(renderFn: (arg0: GeoSearchRenderingOptions, arg1: boolean) => any, unmountFn?: Function): (arg0: CustomGeoSearchWidgetOptions) => any;\r",
    "```\r",
    "\r",
    "</details>"
  ],
  "date": 1576675431
}
