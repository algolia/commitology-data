{
  "hash": "5fb4c5b",
  "author": "Haroen Viaene",
  "subject": "fix(typescript): export correct types (#4476)",
  "bodyLines": [
    "* fix(typescript): allow compatibility of v3 client\r",
    "\r",
    "This PR fixes some miscellaneous typescript issues discovered in angular instantsearch\r",
    "\r",
    "- if JS client v3 is used, it's imperative that the ts-ignore comments stay present, since not the same exports are used. Solution is to double-annotate to keep the multi-line version in exposed to the d.ts (see e.g. https://github.com/microsoft/TypeScript/issues/39520)\r",
    "- similar in places.js\r",
    "- when updating TypeScript, I found that api-extractor can't deal with `import()` types, so those have to be transformed\r",
    "- fix stricter behaviour of tranformItems in hitsPerPage (we send the user the \"render\" values, not the one they entered)\r",
    "- SpeechRecognitionError is no longer exported in lib.d.ts, and is a generic Event now\r",
    "- updated Typescript to 3.8.3. I've tried 3.9.2 as well, but that has a bug with `function()` (transforms to `(arg:any) => ;` instead of `(arg:any) => any;`). I didn't open a TS issue for it, since it's fixed in v4 which we can migrate to later\r",
    "\r",
    "* Update src/connectors/hierarchical-menu/connectHierarchicalMenu.js\r",
    "\r",
    "* move ignore\r",
    "\r",
    "* fix lint\r",
    "\r",
    "* chore(ignore): also work for local\r",
    "\r",
    "* add unused type"
  ],
  "date": 1597405315
}
